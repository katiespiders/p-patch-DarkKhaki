<tr>
  <td><%= image_tag article.pic, class: "thumbnail" %></td>
  <td><%= link_to article.title, article_path(article.id) %> <small><%= article.byline %></small><p><%= article.content[0..500] %> ...</td>
  <td><%= article.comments.count %> comments</td>
</tr>
