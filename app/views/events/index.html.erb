<h1> Calendar </h1>

<%= display_calendar(Date.today) %>

<h3> Upcoming Events </h3>
<% @upcoming_events.each do |event| %>
  <h4><%= event.title %></h4>
  <h5><%= pretty_event_time(event) %></h5>
  <%= event.description %>
<% end %>
